## 任务设定：
构建 本体运动+主动感知+陪聊对话的loop Agent框架：

## 上下文梳理：
1. 梳理lerobot上下文，封装本体运动+主动感知的SDK为skills；
2. 联通loop Agent，实现文本指令，对机器人的运动感知，构建视觉运动闭环控制；
1. 基础的关节运动+相机数据获取，参考：/home/makermods/dexproject/lerobot_skills/example_basic_control.py
2. /home/makermods/dexproject/servo_map.json这个是每个舵机的控制方向的JSON
3. 虚拟环境：new-lerobot
4. 任务目标：左臂尝试用夹爪，抓住白色的盒子，然后移动到右边，松开，放下，然后让右边的夹爪去抓取，再移到再右边。
5. 动作和感知反馈：你可以获取当前的机械臂控制权限，当前的关节角度，两个相机的当前视角；
6. 你要模拟一个Agent的控制环节，根据当前的任务目标，当前状态下的相机视角，关节角度和夹爪状态，然后控制当前机械臂和夹爪的关节增量控制，然后调用底层的skill。注意，每次决策都是非常小的增量控制，用VLM-Agent做视觉伺服。每次决策完，都需要判断，子任务是否完成，如果完成了，切换子任务，如果没完成，则给出下一步的增量控制。注意总结每个关节的控制效果，比如说正负代表的是左右，还是上下。以及具体操作对象是否变得更近，更远，是否抓住。

